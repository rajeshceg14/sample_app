<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
     <section>
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <!--<%if current_user?(@user) %> 
    <h4><%= link_to "Sign out", signout_path, method: "delete" %> </h4>
  <%end%>-->
</div>
<div class="span8">
  <%= render 'follow_form' if signed_in? %>
    <% if @user.microposts.any? %>
      <%= will_paginate @microposts %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
      
    <% end %>
  </div>
  <!--  <p>New user? <%= link_to "Sign up now!", signup_path %></p>  -->


<!--
<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
</h1>


-->
